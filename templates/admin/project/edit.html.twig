{% extends 'base.html.twig' %}

{% block title %}Éditer un Projet{% endblock %}

{% block body %}
    <h1 class="text-center pt-4 text-light">Éditer un projet</h1>
	<a href="{{ path('admin_project_index') }}" class="ms-5 btn btn-primary">
		<i class="bi bi-arrow-left"></i>
		Retour à la liste</a>
	<div class="w-50 mx-auto my-4 text-center">
		{{ include('admin/project/_form.html.twig', {'button_label': 'Éditer'}) }}
	</div>
{% endblock %}
