{# {% block stylesheets %}
	{{ encore_entry_link_tags('cards') }}
{% endblock %} #}

<div class="cardGrey-image col-lg-6 col-md-6 py-2 d-flex justify-content-center">
	<div class="card w-100 m-2 shadow-sm data-aos="zoom-in"">
		<div class="card-body">
			<h5 class="card-title text-center">{{ project.title }}</h5>
			<p class="text-center">
				{{ project.date| date("d/m/Y") }}
			</p>
			<p class="card-text text-center text-truncate">{{ project.description }}</p>
			 <a href="{{ path('projectAp_show', { slug: project.slug }) }}" class="btn btn-secondary d-block mx-auto">Plus d'informations</a> 
		</div>
	</div>
</div>
